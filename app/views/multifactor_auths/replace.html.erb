<%= render partial: "mfa_setup", locals: {_context: "replace", _form_path: replace_multifactor_auth_path, seed: @seed, issuer: issuer, current_user: current_user, key_chunk_length: @key_chunk_length} %>
